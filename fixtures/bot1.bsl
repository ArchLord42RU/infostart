Функция Делай(Текст, ЭтоКоманда, Сообщение) Экспорт
	// Делай(Сообщение.message.text, Сообщение.message.entities.type = "bot_command", Сообщение.message)
	Сообщить(СтрШаблон("
	|
	|%1
	|
	|", Текст));
	Возврат Неопределено;
КонецФункции